{
  "name": "hermes-engineering-os-dashboard",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "build": "node build.mjs",
    "check": "tsc --noEmit",
    "test": "node --test tests/*.test.cjs",
    "test:browser": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "1.62.1",
    "esbuild": "0.28.2",
    "typescript": "7.0.2"
  }
}

